int a;
int b = 3;

int suma(int x, int y) {
    int z;
    z = x + y * 2;
    return z;
}

int main() {
    int n, r;
    n = 5;

    while (n > 0) {
        n = n - 1;
    }

    r = suma(n, b);

    if (r > 0) {
        r = r - 1;
    } else {
        r = r + 1;
    }

    return r;
}
